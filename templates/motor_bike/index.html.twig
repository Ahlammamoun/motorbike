{% extends 'base.html.twig' %}

{% block title %}Hello MotorBikeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<h1 class="text-center">Liste des motos</h1>
<a href="{{ path('app_motor_bike_create')}}" class="btn btn-secondary w-100">Ajouter une moto</a>


<table class="table bg-dark text-white">
    <thead>
    <tr>
      <th >#ID</th>
      <th >Model</th>
      <th >Année</th>   
      <th >Marque</th>
      <th >Picture</th>
      <th >Action</th>
     <tr>
    </thead>
 <tbody>
          {% for motorBike in  motorBikes %}
            
        </tr>
      <th >{{ motorBike.id }}</th> 
      <td >{{ motorBike.name }}</td>
      <td >{{ motorBike.releaseDate|date("Y") }}</td>
      <td >{{ motorBike.brand.name }}</td>  
  <td class="col-4 movie__poster w-25"  style="background-image: url({{ motorBike.picture }}); " ></td> 
     <td>
     <a href="{{ path('app_motorbike',  {'id': motorBike.id}) }}" class="btn btn-secondary">voir la moto</a>
 


    </td>

  {% endfor %}
  </tbody>
</table>

{% endblock %}
