{% extends 'base.html.twig' %}

{% block title_nav %}{% endblock %}

{% block body %}
 <h1 class="text-center">Page de gestion des Marques</h1>
    <button class=" bg-dark  w-100">
    <a href="{{ path('app_back_brand_index') }}" class="text-danger">back to list</a>
    </button>
    <button class=" bg-dark  w-100">    
    <a href="{{ path('app_back_motor_bike_home') }}" class="text-danger">back to general backoffice</a>
    </button>
    <table class="table text-light text-center">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>
 <button class="  bg-dark bg-gradient">
   <a href="{{ path('app_back_brand_edit', {'id': brand.id}) }}" class="text-light">edit</a>
    </button>


    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
